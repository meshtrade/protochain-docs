---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - api-reference/shared-types.mdx
  - snippets/commitment-level-note.mdx
  - snippets/token-program-field.mdx
  - snippets/base58-note.mdx
  - snippets/grpc-connection-go.mdx
  - snippets/grpc-connection-ts.mdx
  - snippets/grpc-connection-rust.mdx
autonomous: true
requirements:
  - FOUND-05
  - FOUND-06
must_haves:
  truths:
    - "A developer reading a method page can link to /api-reference/shared-types to understand CommitmentLevel, TokenProgram, or KeyPair"
    - "CommitmentLevel enum values have descriptions covering speed/reliability trade-offs, not just the enum name"
    - "TokenProgram enum explains LEGACY (SPL Token) vs 2022 (Token-2022) distinction and that UNSPECIFIED is an error"
    - "KeyPair documents both fields and their encoding formats"
    - "Snippet files exist and contain importable MDX content for commitment level, token program, base58, and connection boilerplate"
  artifacts:
    - path: "api-reference/shared-types.mdx"
      provides: "Authoritative shared types reference page"
      contains: "CommitmentLevel, TokenProgram, KeyPair with behavioral descriptions"
    - path: "snippets/commitment-level-note.mdx"
      provides: "Reusable CommitmentLevel parameter note"
    - path: "snippets/token-program-field.mdx"
      provides: "Reusable TokenProgram field description"
    - path: "snippets/base58-note.mdx"
      provides: "Reusable base58 address encoding note"
    - path: "snippets/grpc-connection-go.mdx"
      provides: "Reusable Go gRPC connection setup code"
    - path: "snippets/grpc-connection-ts.mdx"
      provides: "Reusable TypeScript gRPC connection setup code"
    - path: "snippets/grpc-connection-rust.mdx"
      provides: "Reusable Rust gRPC connection setup code"
  key_links:
    - from: "api-reference/shared-types.mdx"
      to: "CommitmentLevel enum values"
      via: "Proto source: ../protochain/lib/proto/protochain/solana/type/v1/commitment_level.proto"
      pattern: "COMMITMENT_LEVEL_PROCESSED.*COMMITMENT_LEVEL_CONFIRMED.*COMMITMENT_LEVEL_FINALIZED"
    - from: "snippets/commitment-level-note.mdx"
      to: "api-reference/shared-types#commitmentlevel"
      via: "markdown link"
      pattern: "shared-types"
---

<objective>
Write the shared types reference page (CommitmentLevel, TokenProgram, KeyPair) and create all 6 reusable MDX snippet files. The shared types page is the cross-link destination for every method page that uses these types. The snippets prevent content drift by centralizing repeated field descriptions.

Purpose: Shared types is the foundation other pages link back to. Writing it now (from the authoritative proto files) means future phases can cross-link immediately rather than using TODO placeholders.
Output: api-reference/shared-types.mdx with behavioral depth; 6 snippet files ready for import.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md

<interfaces>
<!-- Proto definitions — source of truth for shared types content -->
<!-- Read these files directly before writing any enum values or descriptions -->

From ../protochain/lib/proto/protochain/solana/type/v1/commitment_level.proto:
```protobuf
enum CommitmentLevel {
  COMMITMENT_LEVEL_UNSPECIFIED = 0;  // services use their default
  COMMITMENT_LEVEL_PROCESSED = 1;    // included in block, not yet cluster-confirmed — fastest, least reliable
  COMMITMENT_LEVEL_CONFIRMED = 2;    // confirmed by supermajority — good balance of speed/reliability
  COMMITMENT_LEVEL_FINALIZED = 3;    // rooted/finalized — slowest, absolute finality
}
```

From ../protochain/lib/proto/protochain/solana/type/v1/token_program.proto:
```protobuf
enum TokenProgram {
  TOKEN_PROGRAM_UNSPECIFIED = 0;  // INVALID — services return error
  TOKEN_PROGRAM_LEGACY = 1;       // original spl-token program
  TOKEN_PROGRAM_2022 = 2;         // Token Extensions (spl-token-2022), transfer fees, interest-bearing, etc.
}
```

From ../protochain/lib/proto/protochain/solana/type/v1/keypair.proto:
```protobuf
message KeyPair {
  string public_key = 1;   // Base58 encoded public key
  string private_key = 2;  // Hex encoded private key (32 bytes)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the shared types reference page</name>
  <files>api-reference/shared-types.mdx</files>
  <read_first>
    - api-reference/shared-types.mdx — this is currently a stub from Plan 01; replace its content
    - ../protochain/lib/proto/protochain/solana/type/v1/commitment_level.proto — authoritative enum values and comments
    - ../protochain/lib/proto/protochain/solana/type/v1/token_program.proto — authoritative enum values and comments
    - ../protochain/lib/proto/protochain/solana/type/v1/keypair.proto — authoritative message fields
    - .planning/phases/01-foundation/01-CONTEXT.md — locked decisions D-09 (behavioral guidance, no code snippets on this page) and D-10
  </read_first>
  <action>
Replace api-reference/shared-types.mdx with the following full content. This page documents the three types that every service method page will cross-link to. Per D-09: each enum value gets a description, when to use it, and trade-offs. Per D-10: no code snippets on this page — code lives on method pages.

Use Mintlify `ResponseField` component for each type's fields. No `ParamField`. No code blocks on this page.

Write the page with this exact structure:

```mdx
---
title: "Shared Types"
description: "Common types used across all Protochain gRPC services."
---

These types appear as fields in request and response messages across multiple Protochain services. Each type is documented once here so method pages can link back rather than repeating definitions.

## CommitmentLevel

`protochain.solana.type.v1.CommitmentLevel`

CommitmentLevel controls which version of the ledger state a query reads from. Solana validators process blocks in three stages — processed, confirmed, finalized — each representing a higher degree of cluster consensus.

When a method accepts an optional `commitment_level` field, omitting it or passing `COMMITMENT_LEVEL_UNSPECIFIED` lets the service use its default (typically `COMMITMENT_LEVEL_CONFIRMED`).

<ResponseField name="COMMITMENT_LEVEL_UNSPECIFIED" type="enum value = 0">
  Not set. The service uses its default commitment level, which is `COMMITMENT_LEVEL_CONFIRMED` unless otherwise documented. Use this when you have no specific latency or finality requirements.
</ResponseField>

<ResponseField name="COMMITMENT_LEVEL_PROCESSED" type="enum value = 1">
  The transaction has been processed by the leader and included in a block, but the block has not yet been confirmed by the cluster. **Use when:** you need the absolute lowest latency and can tolerate the possibility that the block gets dropped. Not suitable for anything involving fund movements.
</ResponseField>

<ResponseField name="COMMITMENT_LEVEL_CONFIRMED" type="enum value = 2">
  The block containing the transaction has been confirmed by a supermajority of the cluster. **Use when:** you need a good balance of speed and reliability — the default for most operations. Appropriate for reading account balances, checking transaction status during interactive flows.
</ResponseField>

<ResponseField name="COMMITMENT_LEVEL_FINALIZED" type="enum value = 3">
  The block has been rooted (finalized) by the cluster. This block cannot be rolled back. **Use when:** funds must be irreversibly settled before proceeding — for example, confirming a deposit before crediting a user account. This is the slowest option; finality on Solana typically takes 32+ slots (~13 seconds) after confirmation.
</ResponseField>

---

## TokenProgram

`protochain.solana.type.v1.TokenProgram`

TokenProgram identifies which Solana token program an operation targets. SPL Token (`LEGACY`) and Token-2022 (`2022`) are **incompatible** — a mint created with one program cannot be used with the other. You must use the correct program for all operations involving a given token.

<Warning>
  `TOKEN_PROGRAM_UNSPECIFIED` is not a valid value. Services return an error if this value is received. Always specify either `TOKEN_PROGRAM_LEGACY` or `TOKEN_PROGRAM_2022`.
</Warning>

<ResponseField name="TOKEN_PROGRAM_UNSPECIFIED" type="enum value = 0">
  Invalid. Do not use. Services return an error when this value is received.
</ResponseField>

<ResponseField name="TOKEN_PROGRAM_LEGACY" type="enum value = 1">
  The original SPL Token program (`spl-token`, program address `TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA`). The majority of existing Solana tokens use this program. **Use when:** you are working with existing SPL tokens or building for maximum ecosystem compatibility.
</ResponseField>

<ResponseField name="TOKEN_PROGRAM_2022" type="enum value = 2">
  The Token Extensions program (`spl-token-2022`, program address `TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb`), also known as Token-2022. Supports extensions: transfer fees, interest-bearing tokens, non-transferable tokens, confidential transfers, and more. **Use when:** you need Token-2022 extensions, or are creating new tokens and want access to the full extension model.
</ResponseField>

---

## KeyPair

`protochain.solana.type.v1.KeyPair`

A Solana cryptographic key pair used for transaction signing. Returned by `GenerateNewKeyPair` and accepted by `SignTransaction`.

<Warning>
  The `private_key` field is transmitted in plaintext over gRPC. Use TLS in production. Never log or store `private_key` values.
</Warning>

<ResponseField name="public_key" type="string" required>
  Base58-encoded Ed25519 public key. This is the account address on Solana.
</ResponseField>

<ResponseField name="private_key" type="string" required>
  Hex-encoded Ed25519 private key (32 bytes = 64 hex characters). Used to sign transactions.
</ResponseField>
```

Write this content exactly. Do not add sections, do not add code examples, do not add a "See Also" section. The content follows D-09 (behavioral guidance) and D-10 (no code snippets on this page).
  </action>
  <verify>
    <automated>grep -c "CommitmentLevel\|TokenProgram\|KeyPair" /Users/kylesmith/Development/docs/api-reference/shared-types.mdx && grep -c "COMMITMENT_LEVEL_PROCESSED\|COMMITMENT_LEVEL_CONFIRMED\|COMMITMENT_LEVEL_FINALIZED" /Users/kylesmith/Development/docs/api-reference/shared-types.mdx && grep -c "TOKEN_PROGRAM_LEGACY\|TOKEN_PROGRAM_2022" /Users/kylesmith/Development/docs/api-reference/shared-types.mdx</automated>
  </verify>
  <acceptance_criteria>
    - api-reference/shared-types.mdx contains `title: "Shared Types"` in frontmatter
    - File contains `COMMITMENT_LEVEL_PROCESSED`, `COMMITMENT_LEVEL_CONFIRMED`, and `COMMITMENT_LEVEL_FINALIZED` as ResponseField names
    - File contains `TOKEN_PROGRAM_LEGACY` and `TOKEN_PROGRAM_2022` as ResponseField names
    - File contains `TOKEN_PROGRAM_UNSPECIFIED` with warning about it being invalid
    - File contains `public_key` and `private_key` ResponseFields with their encoding types (Base58 and Hex respectively)
    - File contains at least one `<Warning>` callout
    - File does NOT contain any code blocks (no triple backtick fences with language tags on this page — per D-10)
    - `grep "ParamField" /Users/kylesmith/Development/docs/api-reference/shared-types.mdx` returns nothing
  </acceptance_criteria>
  <done>api-reference/shared-types.mdx documents all three shared types with behavioral descriptions, trade-off guidance, and correct field encoding information — ready to be cross-linked from method pages</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable MDX snippets</name>
  <files>
    snippets/commitment-level-note.mdx,
    snippets/token-program-field.mdx,
    snippets/base58-note.mdx,
    snippets/grpc-connection-go.mdx,
    snippets/grpc-connection-ts.mdx,
    snippets/grpc-connection-rust.mdx
  </files>
  <read_first>
    - snippets/snippet-intro.mdx — check if it was deleted in Plan 01 or still exists; if it exists, leave it (it may have been deleted by Task 2 of Plan 01)
    - .planning/research/STACK.md — snippet import syntax (lines 270-285), connection boilerplate examples (lines 82-101), CodeGroup tab label convention ("Go", "Rust", "TypeScript")
    - .planning/phases/01-foundation/01-CONTEXT.md — D-11 (tab order: Go, Rust, TypeScript), D-12 (minimal focused code), D-13 (link to Getting Started for full setup)
  </read_first>
  <action>
Create these 6 snippet files. Each snippet is a standalone MDX fragment that will be imported and rendered inline on method pages. Do not add frontmatter to snippet files — they are fragments, not pages.

FILE 1: snippets/commitment-level-note.mdx
```mdx
<Info>
  **commitment_level** is optional. When omitted or set to `COMMITMENT_LEVEL_UNSPECIFIED`, the service defaults to `COMMITMENT_LEVEL_CONFIRMED`. See [CommitmentLevel](/api-reference/shared-types#commitmentlevel) for trade-offs between processed, confirmed, and finalized.
</Info>
```

FILE 2: snippets/token-program-field.mdx
```mdx
<ResponseField name="token_program" type="TokenProgram" required>
  Specifies which token program to use. Must be either `TOKEN_PROGRAM_LEGACY` (SPL Token) or `TOKEN_PROGRAM_2022` (Token Extensions). `TOKEN_PROGRAM_UNSPECIFIED` is invalid and returns an error. SPL Token and Token-2022 mints are incompatible — use the same program that was used to create the mint. See [TokenProgram](/api-reference/shared-types#tokenprogram).
</ResponseField>
```

FILE 3: snippets/base58-note.mdx
```mdx
<Info>
  Account addresses are Base58-encoded Ed25519 public keys (e.g., `7EcDhSYGxXyscszYEp35KHN8vvw3svAuLKTzXwCFLtV`). Protochain accepts and returns addresses in this format throughout the API.
</Info>
```

FILE 4: snippets/grpc-connection-go.mdx
```mdx
```go Go
import (
    "google.golang.org/grpc"
    "google.golang.org/grpc/credentials/insecure"
    account_v1 "github.com/meshtrade/protochain/lib/go/protochain/solana/account/v1"
)

conn, err := grpc.NewClient("localhost:50051", grpc.WithTransportCredentials(insecure.NewCredentials()))
if err != nil {
    log.Fatal(err)
}
defer conn.Close()

client := account_v1.NewServiceClient(conn)
```
```

FILE 5: snippets/grpc-connection-ts.mdx
```mdx
```typescript TypeScript
import * as grpc from '@grpc/grpc-js';
import { ServiceClient } from '@protochain/solana-account-v1';

const client = new ServiceClient(
  'localhost:50051',
  grpc.credentials.createInsecure()
);
```
```

FILE 6: snippets/grpc-connection-rust.mdx
```mdx
```rust Rust
use protochain::solana::account::v1::service_client::ServiceClient;

let mut client = ServiceClient::connect("http://localhost:50051").await?;
```
```

IMPORTANT: The connection snippets show the pattern for the Account Service. When used on other service pages, the executor for that page should adapt the import path and client type for the relevant service — the snippets are a starting-point template, not import-and-done.

For the connection snippets (files 4–6), wrap the code block in a `<Note>` with a link to Getting Started per D-13:

After the code block in each connection snippet, add:
```
<Note>See [Connecting to Protochain](/guides/connecting) for TLS setup, authentication, and production configuration.</Note>
```
  </action>
  <verify>
    <automated>ls /Users/kylesmith/Development/docs/snippets/ && grep -l "CommitmentLevel\|commitment_level" /Users/kylesmith/Development/docs/snippets/ && grep -l "TokenProgram\|token_program" /Users/kylesmith/Development/docs/snippets/ && grep -l "Base58\|base58" /Users/kylesmith/Development/docs/snippets/ && grep -l "grpc.NewClient\|grpc.Dial\|grpc.credentials" /Users/kylesmith/Development/docs/snippets/</automated>
  </verify>
  <acceptance_criteria>
    - snippets/commitment-level-note.mdx exists and contains "COMMITMENT_LEVEL_CONFIRMED" and a link containing "shared-types"
    - snippets/token-program-field.mdx exists and contains "TOKEN_PROGRAM_LEGACY", "TOKEN_PROGRAM_2022", and "TOKEN_PROGRAM_UNSPECIFIED"
    - snippets/base58-note.mdx exists and contains "Base58"
    - snippets/grpc-connection-go.mdx exists and contains "go Go" code fence and "grpc.NewClient" or similar grpc connection call
    - snippets/grpc-connection-ts.mdx exists and contains "typescript TypeScript" code fence and "@grpc/grpc-js"
    - snippets/grpc-connection-rust.mdx exists and contains "rust Rust" code fence and "ServiceClient::connect"
    - None of the 6 snippet files contain YAML frontmatter (no `---` at line 1)
    - Each connection snippet contains a `<Note>` referencing "/guides/connecting"
  </acceptance_criteria>
  <done>All 6 reusable snippet files exist and contain importable MDX content ready for use by Phase 2+ method pages</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "ResponseField" /Users/kylesmith/Development/docs/api-reference/shared-types.mdx` returns 7 or more (3 CommitmentLevel + 2 TokenProgram + 2 KeyPair)
2. `ls /Users/kylesmith/Development/docs/snippets/` shows all 6 new snippet files
3. `grep "ParamField" /Users/kylesmith/Development/docs/api-reference/shared-types.mdx` returns nothing
4. `grep "COMMITMENT_LEVEL_UNSPECIFIED" /Users/kylesmith/Development/docs/snippets/commitment-level-note.mdx` returns a result
5. `grep "TOKEN_PROGRAM_UNSPECIFIED is invalid" /Users/kylesmith/Development/docs/snippets/token-program-field.mdx` or similar warning text exists
</verification>

<success_criteria>
- api-reference/shared-types.mdx documents CommitmentLevel (4 values with trade-off guidance), TokenProgram (3 values with incompatibility warning), KeyPair (2 fields with encoding types) — satisfies FOUND-05
- All 6 snippet files created and contain correct content for reuse — satisfies FOUND-06
- Shared types page has behavioral depth per D-09: each enum value explains when to use it and the trade-offs
- No code snippets on shared-types.mdx per D-10
- Snippet content uses ResponseField (not ParamField) per STACK.md recommendation
</success_criteria>

<output>
After completion, create `/Users/kylesmith/Development/docs/.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
