---
phase: 04-program-services-and-error-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api-reference/system-program/overview.mdx
  - api-reference/system-program/create.mdx
  - api-reference/system-program/transfer.mdx
  - docs.json
autonomous: true
requirements:
  - SYS-01
  - SYS-02
  - SYS-03

must_haves:
  truths:
    - "A developer visiting the System Program Service overview sees a method table and understands these methods return SolanaInstruction, not execute on-chain"
    - "The Create page documents both Create and CreateWithSeed with all request fields, a SolanaInstruction response, and Go/Rust/TypeScript code examples"
    - "The Transfer page documents both Transfer and TransferWithSeed with all request fields, a SolanaInstruction response, and Go/Rust/TypeScript code examples"
    - "Every instruction method page has the brief Note callout linking to Instructions & Transactions"
    - "docs.json no longer lists the removed stub pages (create-with-seed, transfer-with-seed)"
  artifacts:
    - path: "api-reference/system-program/overview.mdx"
      provides: "System Program Service overview with instruction builder pattern explanation and method table"
      contains: "SolanaInstruction"
    - path: "api-reference/system-program/create.mdx"
      provides: "Create and CreateWithSeed method documentation"
      contains: "CreateWithSeed"
    - path: "api-reference/system-program/transfer.mdx"
      provides: "Transfer and TransferWithSeed method documentation"
      contains: "TransferWithSeed"
  key_links:
    - from: "api-reference/system-program/overview.mdx"
      to: "/concepts/instructions-and-transactions"
      via: "inline link in instruction builder explanation"
    - from: "api-reference/system-program/create.mdx"
      to: "/concepts/instructions-and-transactions"
      via: "Note callout"
    - from: "api-reference/system-program/transfer.mdx"
      to: "/concepts/instructions-and-transactions"
      via: "Note callout"
---

<objective>
Write the System Program Service overview page and the Create/Transfer grouped method pages. These are instruction builder methods — they return SolanaInstruction objects, not execute on-chain.

Purpose: Developers working with the System Program (creating accounts, transferring SOL) need clear reference pages that explain the instruction builder pattern and document every field.

Output: 3 written MDX pages + docs.json updates removing the now-redundant create-with-seed and transfer-with-seed individual stub pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-program-services-and-error-reference/04-CONTEXT.md

<!-- Pattern reference -->
@api-reference/account/get-account.mdx
@api-reference/transaction/overview.mdx
@snippets/commitment-level-note.mdx
</context>

<interfaces>
<!-- From ../protochain/lib/proto/protochain/solana/program/system/v1/service.proto -->
<!-- Go package: github.com/meshtrade/protochain/lib/go/protochain/solana/program/system/v1;system_v1 -->

Create/CreateWithSeed request/response:
- CreateRequest: payer (string), new_account (string), owner (string), lamports (uint64), space (uint64)
- CreateWithSeedRequest: payer (string), new_account (string), base (string), seed (string), lamports (uint64), space (uint64)
- Both responses: instruction (SolanaInstruction)

Transfer/TransferWithSeed request/response:
- TransferRequest: from (string), to (string), lamports (uint64)
- TransferWithSeedRequest: from (string), from_base (string), from_seed (string), to (string), lamports (uint64)
- Both responses: instruction (SolanaInstruction)

All 11 System Program methods (for overview table):
Create, Transfer, Allocate, Assign, CreateWithSeed, AllocateWithSeed, AssignWithSeed,
TransferWithSeed, InitializeNonceAccount, AuthorizeNonceAccount, WithdrawNonceAccount,
AdvanceNonceAccount, UpgradeNonceAccount
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write System Program Service overview page</name>
  <files>api-reference/system-program/overview.mdx</files>
  <read_first>
    - api-reference/transaction/overview.mdx (overview page pattern)
    - api-reference/system-program/overview.mdx (current stub to replace)
    - .planning/phases/04-program-services-and-error-reference/04-CONTEXT.md (decisions D-01 through D-13)
  </read_first>
  <action>
Replace the stub with a full overview page following the Transaction Service overview pattern.

Content to include:
1. Opening paragraph: Explain that System Program Service methods are instruction builders — each method returns a SolanaInstruction that you add to a transaction, compile, sign, and submit via Transaction Service. Link to /concepts/instructions-and-transactions (per D-06 context).
2. Note callout: "System Program methods return SolanaInstruction objects. To execute them on-chain, add the instructions to a transaction and use the Transaction Service. See [Instructions & Transactions](/concepts/instructions-and-transactions)."
3. Methods table with columns: Method | Description | Grouped Page. Group the methods to reflect the grouped page structure (per D-01):
   - Create / CreateWithSeed | Create a new account or a seed-derived account | /api-reference/system-program/create
   - Transfer / TransferWithSeed | Transfer SOL or transfer from a seed-derived account | /api-reference/system-program/transfer
   - Allocate / AllocateWithSeed | Allocate space for an account | /api-reference/system-program/allocate
   - Assign / AssignWithSeed | Change the owner of an account | /api-reference/system-program/assign
   - Nonce Operations (5 methods) | Initialize, authorize, withdraw, advance, upgrade nonce accounts for durable transactions | /api-reference/system-program/nonce-operations

Frontmatter: title "System Program Service", description "Build Solana System Program instructions for account creation, SOL transfers, space allocation, and durable nonce management."

Tone: concise, developer-focused (per D-13). No fluff.
  </action>
  <verify>
    <automated>grep -c "SolanaInstruction\|instructions-and-transactions\|CreateWithSeed\|nonce" /Users/kylesmith/Development/docs/api-reference/system-program/overview.mdx</automated>
  </verify>
  <acceptance_criteria>
    - File contains "SolanaInstruction" (instruction builder pattern explained)
    - File contains "/concepts/instructions-and-transactions" link
    - File contains all 5 grouped method rows in a table
    - File contains "nonce" (nonce operations covered in table)
    - File does NOT contain "under construction" or "Coming soon"
  </acceptance_criteria>
  <done>Overview page explains instruction builder pattern and lists all grouped method pages with descriptions.</done>
</task>

<task type="auto">
  <name>Task 2: Write Create / CreateWithSeed grouped method page</name>
  <files>api-reference/system-program/create.mdx</files>
  <read_first>
    - api-reference/account/get-account.mdx (method page template with ResponseField, CodeGroup pattern)
    - api-reference/system-program/create.mdx (current stub to replace)
    - .planning/phases/04-program-services-and-error-reference/04-CONTEXT.md (D-05, D-06, D-08 through D-13)
  </read_first>
  <action>
Replace the stub. This page covers two methods: Create and CreateWithSeed.

Page structure:
1. Frontmatter: title "Create Account", description "Build a System Program instruction to create a new Solana account, with or without a seed."
2. Opening sentence: What Create does (creates a new account funded with lamports, owned by a program).
3. Note callout (per D-06): "This method returns a SolanaInstruction. Add it to a transaction and use the Transaction Service to execute it on-chain. See [Instructions & Transactions](/concepts/instructions-and-transactions)."

## Create

Brief description: Creates a new Solana account funded with lamports and assigned to an owner program.

### Request
ResponseField for each field (per D-08, D-09 developer-friendly types):
- payer: "Base58-encoded public key (string)" required — "The account paying for creation. Must be a signer."
- new_account: "Base58-encoded public key (string)" required — "The new account's public key. Must be a signer."
- owner: "Base58-encoded public key (string)" — "Program that will own the new account. Defaults to the System Program."
- lamports: "uint64" required — "Lamports to deposit into the new account. Must cover rent exemption. Use [GetMinimumBalanceForRentExemption](/api-reference/rpc-client/get-minimum-balance-for-rent-exemption) to calculate."
- space: "uint64" required — "Bytes to allocate for the account's data. Use 0 for wallet accounts with no data."

### Response
ResponseField:
- instruction: "SolanaInstruction (object)" — "The System Program create_account instruction. Add to a transaction via CompileTransaction."

### Code Examples
CodeGroup with Go, Rust, TypeScript tabs (per D-10, D-11):
- Go: `client.Create(ctx, &system_v1.CreateRequest{Payer: "...", NewAccount: "...", Lamports: 1000000, Space: 0})`
- Rust: `client.create(tonic::Request::new(CreateRequest { payer: "...".to_string(), new_account: "...".to_string(), lamports: 1_000_000, space: 0, ..Default::default() })).await?`
- TypeScript: `client.create(req, callback)` with setters

End code examples with comment: `// Add instruction to your transaction` (per D-05).

---

## CreateWithSeed

Brief description: Creates an account at an address derived from a base public key and seed string. Useful when the account address must be deterministic.

### Request
ResponseField for each field:
- payer: "Base58-encoded public key (string)" required — payer
- new_account: "Base58-encoded public key (string)" required — "The derived account address. Must match PublicKey.createWithSeed(base, seed, owner)."
- base: "Base58-encoded public key (string)" required — "Base public key used to derive new_account."
- seed: "string" required — "Seed string (max 32 bytes) used to derive new_account."
- lamports: "uint64" required — lamports for rent exemption
- space: "uint64" required — bytes to allocate

### Response
ResponseField:
- instruction: "SolanaInstruction (object)" — the instruction

### Code Examples
CodeGroup Go/Rust/TypeScript showing CreateWithSeed call with seed parameter.
  </action>
  <verify>
    <automated>grep -c "CreateWithSeed\|instructions-and-transactions\|SolanaInstruction\|payer\|lamports" /Users/kylesmith/Development/docs/api-reference/system-program/create.mdx</automated>
  </verify>
  <acceptance_criteria>
    - File contains "CreateWithSeed" section (both methods documented)
    - File contains "/concepts/instructions-and-transactions" link in Note callout
    - File contains "SolanaInstruction" in response field description
    - File contains "payer" ResponseField for both methods
    - File contains CodeGroup with Go, Rust, TypeScript tabs for both methods
    - File does NOT contain "under construction" or "Coming soon"
  </acceptance_criteria>
  <done>create.mdx covers both Create and CreateWithSeed with request fields, response field, and three-language code examples. Note callout links to Instructions & Transactions.</done>
</task>

<task type="auto">
  <name>Task 3: Write Transfer / TransferWithSeed grouped method page + update docs.json</name>
  <files>api-reference/system-program/transfer.mdx, docs.json</files>
  <read_first>
    - api-reference/system-program/transfer.mdx (current stub to replace)
    - api-reference/system-program/create.mdx (use as template — just written in Task 2)
    - docs.json (update navigation to remove individual seed variant stubs)
    - .planning/phases/04-program-services-and-error-reference/04-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
Part A — Write transfer.mdx following the exact same structure as create.mdx:

1. Frontmatter: title "Transfer SOL", description "Build a System Program instruction to transfer SOL between accounts, with or without a seed-derived sender."
2. Note callout: "This method returns a SolanaInstruction. Add it to a transaction and use the Transaction Service to execute it on-chain. See [Instructions & Transactions](/concepts/instructions-and-transactions)."

## Transfer

Description: Transfers SOL (in lamports) from one account to another.

### Request
- from: "Base58-encoded public key (string)" required — "The sender account. Must be a signer."
- to: "Base58-encoded public key (string)" required — "The recipient account."
- lamports: "uint64" required — "Amount of SOL to transfer, in lamports. 1 SOL = 1,000,000,000 lamports."

### Response
- instruction: "SolanaInstruction (object)" — the transfer instruction

### Code Examples
CodeGroup Go/Rust/TypeScript with Transfer call. End with `// Add instruction to your transaction`.

---

## TransferWithSeed

Description: Transfers SOL from a seed-derived account. The sending account's address is derived from a base public key and seed, so the base key (not the derived address) signs the transaction.

### Request
- from: "Base58-encoded public key (string)" required — "The seed-derived sender account address."
- from_base: "Base58-encoded public key (string)" required — "Base public key used to derive `from`. This account must be a signer."
- from_seed: "string" required — "Seed string used to derive `from`."
- to: "Base58-encoded public key (string)" required — "The recipient account."
- lamports: "uint64" required — "Amount to transfer, in lamports."

### Response
- instruction: "SolanaInstruction (object)" — the instruction

### Code Examples
CodeGroup Go/Rust/TypeScript with TransferWithSeed call.

---

Part B — Update docs.json:
In the "System Program Service" group pages array, replace the current flat list with the grouped structure. Remove individual seed-variant stub entries and replace with the grouped page names. The resulting pages array should be:

```json
[
  "api-reference/system-program/overview",
  "api-reference/system-program/create",
  "api-reference/system-program/transfer",
  "api-reference/system-program/allocate",
  "api-reference/system-program/assign",
  "api-reference/system-program/nonce-operations"
]
```

Remove these entries: "api-reference/system-program/create-with-seed", "api-reference/system-program/allocate-with-seed", "api-reference/system-program/assign-with-seed", "api-reference/system-program/transfer-with-seed", "api-reference/system-program/initialize-nonce-account", "api-reference/system-program/authorize-nonce-account", "api-reference/system-program/withdraw-nonce-account", "api-reference/system-program/advance-nonce-account", "api-reference/system-program/upgrade-nonce-account".

Note: "nonce-operations" page will be created by Plan 02. The docs.json entry can be added now — Mintlify will show a warning for missing pages but won't break existing pages.

Also update the "Errors" group to add the error-handling-patterns page (will be created by Plan 04):
```json
{
  "group": "Errors",
  "pages": [
    "api-reference/errors",
    "api-reference/error-handling-patterns"
  ]
}
```
  </action>
  <verify>
    <automated>grep -c "TransferWithSeed\|from_base\|from_seed\|instructions-and-transactions" /Users/kylesmith/Development/docs/api-reference/system-program/transfer.mdx && grep -c "nonce-operations" /Users/kylesmith/Development/docs/docs.json</automated>
  </verify>
  <acceptance_criteria>
    - transfer.mdx contains "TransferWithSeed" section with from_base and from_seed fields documented
    - transfer.mdx contains Note callout linking to "/concepts/instructions-and-transactions"
    - transfer.mdx contains CodeGroup with Go, Rust, TypeScript for both methods
    - docs.json System Program Service pages array contains "api-reference/system-program/nonce-operations"
    - docs.json System Program Service pages array does NOT contain "create-with-seed" or "transfer-with-seed" as individual entries
    - docs.json Errors group contains "api-reference/error-handling-patterns"
    - docs.json is valid JSON (verify with: node -e "require('./docs.json')" from repo root)
  </acceptance_criteria>
  <done>transfer.mdx covers Transfer and TransferWithSeed. docs.json updated to reflect grouped navigation structure with nonce-operations and error-handling-patterns entries added.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -r "Coming soon\|under construction" api-reference/system-program/overview.mdx api-reference/system-program/create.mdx api-reference/system-program/transfer.mdx` — must return no matches
2. `node -e "require('./docs.json')"` from repo root — must succeed (valid JSON)
3. `grep "nonce-operations" docs.json` — must match
4. `grep "error-handling-patterns" docs.json` — must match
5. `grep -c "SolanaInstruction" api-reference/system-program/create.mdx api-reference/system-program/transfer.mdx` — each must be > 0
</verification>

<success_criteria>
- System Program Service overview is complete with instruction builder explanation and grouped method table
- Create page covers both Create and CreateWithSeed with full field documentation and code examples
- Transfer page covers both Transfer and TransferWithSeed with full field documentation and code examples
- docs.json updated: System Program group uses grouped page names, Errors group includes error-handling-patterns
- No "Coming soon" or stub content remains in the three written pages
</success_criteria>

<output>
After completion, create `.planning/phases/04-program-services-and-error-reference/04-01-SUMMARY.md`
</output>
