---
phase: 04-program-services-and-error-reference
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api-reference/system-program/allocate.mdx
  - api-reference/system-program/assign.mdx
  - api-reference/system-program/nonce-operations.mdx
autonomous: true
requirements:
  - SYS-04
  - SYS-05
  - SYS-06

must_haves:
  truths:
    - "The Allocate page documents both Allocate and AllocateWithSeed with request fields and code examples"
    - "The Assign page documents both Assign and AssignWithSeed with request fields and code examples"
    - "The Nonce Operations page explains when to use nonce accounts (durable transactions) before listing all 5 methods"
    - "Every instruction method page has the brief Note callout linking to Instructions & Transactions"
    - "nonce-operations.mdx is a new file (not an existing stub)"
  artifacts:
    - path: "api-reference/system-program/allocate.mdx"
      provides: "Allocate and AllocateWithSeed documentation"
      contains: "AllocateWithSeed"
    - path: "api-reference/system-program/assign.mdx"
      provides: "Assign and AssignWithSeed documentation"
      contains: "AssignWithSeed"
    - path: "api-reference/system-program/nonce-operations.mdx"
      provides: "All 5 nonce operation methods on one page"
      contains: "InitializeNonceAccount"
  key_links:
    - from: "api-reference/system-program/allocate.mdx"
      to: "/concepts/instructions-and-transactions"
      via: "Note callout"
    - from: "api-reference/system-program/assign.mdx"
      to: "/concepts/instructions-and-transactions"
      via: "Note callout"
    - from: "api-reference/system-program/nonce-operations.mdx"
      to: "/concepts/instructions-and-transactions"
      via: "Note callout"
---

<objective>
Write the Allocate/AllocateWithSeed grouped page, the Assign/AssignWithSeed grouped page, and the Nonce Operations page covering all 5 nonce methods. These complete the System Program Service documentation.

Purpose: Developers managing account space (Allocate), reassigning account ownership (Assign), or building durable transactions using nonce accounts need complete reference documentation.

Output: 3 new/replaced MDX files. The nonce-operations.mdx is a net-new file (replacing 5 individual stubs that were removed from docs.json by Plan 01).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-program-services-and-error-reference/04-CONTEXT.md
@api-reference/system-program/create.mdx
@api-reference/account/get-account.mdx
</context>

<interfaces>
<!-- From ../protochain/lib/proto/protochain/solana/program/system/v1/service.proto -->

AllocateRequest: account (string), space (uint64)
AllocateWithSeedRequest: account (string), base (string), seed (string), space (uint64)
Both responses: instruction (SolanaInstruction)

AssignRequest: account (string), owner_program (string)
AssignWithSeedRequest: account (string), base (string), seed (string), owner_program (string)
Both responses: instruction (SolanaInstruction)

Nonce operation requests/responses:
- InitializeNonceAccountRequest: nonce_account (string), authority (string) → instruction (SolanaInstruction)
- AuthorizeNonceAccountRequest: nonce_account (string), current_authority (string), new_authority (string) → instruction
- WithdrawNonceAccountRequest: nonce_account (string), authority (string), to (string), lamports (uint64) → instruction
- AdvanceNonceAccountRequest: nonce_account (string), authority (string) → instruction
- UpgradeNonceAccountRequest: nonce_account (string) → instruction
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write Allocate / AllocateWithSeed grouped method page</name>
  <files>api-reference/system-program/allocate.mdx</files>
  <read_first>
    - api-reference/system-program/allocate.mdx (current stub to replace)
    - api-reference/system-program/create.mdx (use as structural template)
    - .planning/phases/04-program-services-and-error-reference/04-CONTEXT.md (D-05, D-06, D-08 through D-13)
  </read_first>
  <action>
Replace the stub with full content. Follow the same page structure as create.mdx.

Frontmatter: title "Allocate Space", description "Build a System Program instruction to allocate data space for an account, with or without a seed."

Note callout (per D-06): "This method returns a SolanaInstruction. Add it to a transaction and use the Transaction Service to execute it on-chain. See [Instructions & Transactions](/concepts/instructions-and-transactions)."

## Allocate

Description: Allocates a specific number of bytes of data space to an existing account. The account must be a signer. Use this before assigning the account to a program that requires specific data storage.

### Request
- account: "Base58-encoded public key (string)" required — "The account to allocate space for. Must be a signer."
- space: "uint64" required — "Number of bytes to allocate. Programs typically have fixed data layout sizes."

### Response
- instruction: "SolanaInstruction (object)" — the allocate instruction

### Code Examples
CodeGroup Go/Rust/TypeScript showing Allocate call. End with `// Add instruction to your transaction`.

---

## AllocateWithSeed

Description: Allocates space for a seed-derived account. The base key must sign the transaction (not the derived account address).

### Request
- account: "Base58-encoded public key (string)" required — "The seed-derived account address to allocate space for."
- base: "Base58-encoded public key (string)" required — "Base public key used to derive `account`. Must be a signer."
- seed: "string" required — "Seed string used to derive `account`."
- space: "uint64" required — "Number of bytes to allocate."

### Response
- instruction: "SolanaInstruction (object)" — the instruction

### Code Examples
CodeGroup Go/Rust/TypeScript with AllocateWithSeed call.
  </action>
  <verify>
    <automated>grep -c "AllocateWithSeed\|instructions-and-transactions\|SolanaInstruction" /Users/kylesmith/Development/docs/api-reference/system-program/allocate.mdx</automated>
  </verify>
  <acceptance_criteria>
    - File contains "AllocateWithSeed" section
    - File contains Note callout linking to "/concepts/instructions-and-transactions"
    - File contains "SolanaInstruction" in response description
    - File contains CodeGroup with Go, Rust, TypeScript for both methods
    - File does NOT contain "under construction" or "Coming soon"
  </acceptance_criteria>
  <done>allocate.mdx covers both Allocate and AllocateWithSeed with full field documentation and code examples.</done>
</task>

<task type="auto">
  <name>Task 2: Write Assign / AssignWithSeed grouped method page</name>
  <files>api-reference/system-program/assign.mdx</files>
  <read_first>
    - api-reference/system-program/assign.mdx (current stub to replace)
    - api-reference/system-program/allocate.mdx (use as template — just written in Task 1)
  </read_first>
  <action>
Replace the stub. Follow the same structure as allocate.mdx.

Frontmatter: title "Assign Owner", description "Build a System Program instruction to change an account's owner program, with or without a seed."

Note callout: "This method returns a SolanaInstruction. Add it to a transaction and use the Transaction Service to execute it on-chain. See [Instructions & Transactions](/concepts/instructions-and-transactions)."

## Assign

Description: Changes the owner program of an account. Only the account's current owner program or System Program can assign a new owner. The account must be a signer.

### Request
- account: "Base58-encoded public key (string)" required — "The account to reassign. Must be a signer."
- owner_program: "Base58-encoded public key (string)" required — "The new owner program address."

### Response
- instruction: "SolanaInstruction (object)" — the assign instruction

### Code Examples
CodeGroup Go/Rust/TypeScript with Assign call. End with `// Add instruction to your transaction`.

---

## AssignWithSeed

Description: Reassigns the owner of a seed-derived account. The base key must sign the transaction.

### Request
- account: "Base58-encoded public key (string)" required — "The seed-derived account address to reassign."
- base: "Base58-encoded public key (string)" required — "Base public key used to derive `account`. Must be a signer."
- seed: "string" required — "Seed string used to derive `account`."
- owner_program: "Base58-encoded public key (string)" required — "The new owner program address."

### Response
- instruction: "SolanaInstruction (object)" — the instruction

### Code Examples
CodeGroup Go/Rust/TypeScript with AssignWithSeed call.
  </action>
  <verify>
    <automated>grep -c "AssignWithSeed\|instructions-and-transactions\|owner_program" /Users/kylesmith/Development/docs/api-reference/system-program/assign.mdx</automated>
  </verify>
  <acceptance_criteria>
    - File contains "AssignWithSeed" section
    - File contains Note callout linking to "/concepts/instructions-and-transactions"
    - File contains "owner_program" ResponseField for both methods
    - File contains CodeGroup with Go, Rust, TypeScript for both methods
    - File does NOT contain "under construction" or "Coming soon"
  </acceptance_criteria>
  <done>assign.mdx covers both Assign and AssignWithSeed with full field documentation and code examples.</done>
</task>

<task type="auto">
  <name>Task 3: Write Nonce Operations page (all 5 nonce methods)</name>
  <files>api-reference/system-program/nonce-operations.mdx</files>
  <read_first>
    - api-reference/system-program/create.mdx (method page template pattern)
    - .planning/phases/04-program-services-and-error-reference/04-CONTEXT.md (specifics: nonce page should explain when to use nonces before listing methods)
    - concepts/transaction-lifecycle.mdx (nonce accounts relate to durable transactions)
  </read_first>
  <action>
Create a new file (does not currently exist). This page covers 5 methods: InitializeNonceAccount, AuthorizeNonceAccount, WithdrawNonceAccount, AdvanceNonceAccount, UpgradeNonceAccount.

Frontmatter: title "Nonce Operations", description "Build System Program instructions to create and manage nonce accounts for durable, expiration-resistant Solana transactions."

Opening section (why nonces — per CONTEXT.md specifics):
Write 2-3 sentences explaining the problem nonce accounts solve: regular Solana transactions expire after ~150 slots (~60 seconds) because they include a recent blockhash. Nonce accounts store a durable blockhash that doesn't expire, enabling transactions to be signed offline, stored, and submitted later. This is essential for multi-sig workflows, hardware signing flows, and scheduled transactions.

Note callout: "All nonce operation methods return SolanaInstruction objects. Add them to a transaction and use the Transaction Service to execute on-chain. See [Instructions & Transactions](/concepts/instructions-and-transactions)."

---

## InitializeNonceAccount

Description: Initializes a nonce account, storing a durable blockhash. The nonce account must already exist with enough lamports for rent exemption and allocated space (typically use System Program Create with `space: 80`).

### Request
- nonce_account: "Base58-encoded public key (string)" required — "The account to initialize as a nonce account. Must already exist."
- authority: "Base58-encoded public key (string)" required — "The authority that can authorize future nonce operations."

### Response
- instruction: "SolanaInstruction (object)" — the initialize_nonce_account instruction

### Code Examples
CodeGroup Go/Rust/TypeScript with InitializeNonceAccount call. End with `// Add instruction to your transaction`.

---

## AuthorizeNonceAccount

Description: Changes the authority of a nonce account. The current authority must sign the transaction.

### Request
- nonce_account: "Base58-encoded public key (string)" required — "The nonce account to update."
- current_authority: "Base58-encoded public key (string)" required — "The current authority. Must be a signer."
- new_authority: "Base58-encoded public key (string)" required — "The new authority."

### Response
- instruction: "SolanaInstruction (object)"

### Code Examples
CodeGroup Go/Rust/TypeScript.

---

## WithdrawNonceAccount

Description: Withdraws lamports from a nonce account back to a destination. If the withdrawal leaves the account below rent-exempt balance, the account is closed.

### Request
- nonce_account: "Base58-encoded public key (string)" required — "The nonce account to withdraw from."
- authority: "Base58-encoded public key (string)" required — "The nonce account's authority. Must be a signer."
- to: "Base58-encoded public key (string)" required — "Destination account for the withdrawn lamports."
- lamports: "uint64" required — "Amount to withdraw, in lamports."

### Response
- instruction: "SolanaInstruction (object)"

### Code Examples
CodeGroup Go/Rust/TypeScript.

---

## AdvanceNonceAccount

Description: Advances the nonce account to use the next blockhash. Include this as the first instruction in any transaction using a nonce account — it invalidates the current nonce value to prevent replay attacks.

### Request
- nonce_account: "Base58-encoded public key (string)" required — "The nonce account to advance."
- authority: "Base58-encoded public key (string)" required — "The nonce account's authority. Must be a signer."

### Response
- instruction: "SolanaInstruction (object)"

### Code Examples
CodeGroup Go/Rust/TypeScript.

---

## UpgradeNonceAccount

Description: Upgrades a legacy nonce account to use the current nonce account format. Only relevant for nonce accounts created before the nonce format upgrade on mainnet.

### Request
- nonce_account: "Base58-encoded public key (string)" required — "The legacy nonce account to upgrade."

### Response
- instruction: "SolanaInstruction (object)"

### Code Examples
CodeGroup Go/Rust/TypeScript.
  </action>
  <verify>
    <automated>grep -c "InitializeNonceAccount\|AuthorizeNonceAccount\|WithdrawNonceAccount\|AdvanceNonceAccount\|UpgradeNonceAccount" /Users/kylesmith/Development/docs/api-reference/system-program/nonce-operations.mdx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at api-reference/system-program/nonce-operations.mdx
    - grep count returns 5 (all 5 nonce method names present)
    - File contains opening prose explaining durable transactions / why nonces exist (contains "blockhash" and "expire" or "expir")
    - File contains Note callout linking to "/concepts/instructions-and-transactions"
    - File contains CodeGroup with Go, Rust, TypeScript tabs for each method
    - File does NOT contain "under construction" or "Coming soon"
  </acceptance_criteria>
  <done>nonce-operations.mdx covers all 5 nonce methods with use-case context, complete field documentation, and code examples.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -r "Coming soon\|under construction" api-reference/system-program/allocate.mdx api-reference/system-program/assign.mdx api-reference/system-program/nonce-operations.mdx` — must return no matches
2. `grep -c "instructions-and-transactions" api-reference/system-program/allocate.mdx api-reference/system-program/assign.mdx api-reference/system-program/nonce-operations.mdx` — each must be ≥ 1
3. `ls api-reference/system-program/nonce-operations.mdx` — file must exist
</verification>

<success_criteria>
- Allocate page covers both Allocate and AllocateWithSeed with all fields documented
- Assign page covers both Assign and AssignWithSeed with all fields documented
- Nonce Operations page covers all 5 nonce methods with use-case context and full field documentation
- Every page has the Note callout linking to Instructions & Transactions
- All three pages have Go/Rust/TypeScript code examples
</success_criteria>

<output>
After completion, create `.planning/phases/04-program-services-and-error-reference/04-02-SUMMARY.md`
</output>
