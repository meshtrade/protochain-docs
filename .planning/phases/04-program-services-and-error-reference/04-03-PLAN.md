---
phase: 04-program-services-and-error-reference
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - api-reference/token-program/overview.mdx
  - api-reference/token-program/mint-creation.mdx
  - api-reference/token-program/holding-accounts.mdx
  - api-reference/token-program/mint.mdx
  - api-reference/token-program/parse-mint.mdx
  - docs.json
autonomous: true
requirements:
  - TOK-01
  - TOK-02
  - TOK-03
  - TOK-04
  - TOK-05
  - TOK-06

must_haves:
  truths:
    - "The Token Program overview makes SPL Token vs Token-2022 incompatibility clear before any method page is encountered"
    - "The Mint Creation page covers both CreateToken2022Mint and CreateSPLTokenMint and highlights the token_program choice prominently"
    - "The Holding Accounts page covers both CreateToken2022HoldingAccount and CreateSPLTokenHoldingAccount"
    - "The Mint page documents the token-program-agnostic MintRequest with the destination_owner_pub_key gotcha (pass owner, not ATA)"
    - "The ParseMint page documents MintInfo and the token_program field in the response"
    - "Every instruction method page has the Note callout linking to Instructions & Transactions"
    - "Token Program methods that are true instruction builders (Mint Creation, Holding Accounts, Mint) have the Note callout; ParseMint does NOT (it is a query, not an instruction builder)"
    - "docs.json Token Program group updated to use grouped page names (mint-creation, holding-accounts)"
  artifacts:
    - path: "api-reference/token-program/overview.mdx"
      provides: "Token Program overview with SPL vs Token-2022 explanation and grouped method table"
      contains: "SPL Token"
    - path: "api-reference/token-program/mint-creation.mdx"
      provides: "CreateToken2022Mint and CreateSPLTokenMint on one page"
      contains: "CreateSPLTokenMint"
    - path: "api-reference/token-program/holding-accounts.mdx"
      provides: "CreateToken2022HoldingAccount and CreateSPLTokenHoldingAccount on one page"
      contains: "CreateSPLTokenHoldingAccount"
    - path: "api-reference/token-program/mint.mdx"
      provides: "Mint method documentation"
      contains: "destination_owner_pub_key"
    - path: "api-reference/token-program/parse-mint.mdx"
      provides: "ParseMint method documentation with MintInfo response"
      contains: "MintInfo"
  key_links:
    - from: "api-reference/token-program/overview.mdx"
      to: "/concepts/token-programs"
      via: "link in SPL vs Token-2022 explanation"
    - from: "api-reference/token-program/mint-creation.mdx"
      to: "/concepts/instructions-and-transactions"
      via: "Note callout"
    - from: "api-reference/token-program/holding-accounts.mdx"
      to: "/concepts/instructions-and-transactions"
      via: "Note callout"
    - from: "api-reference/token-program/mint.mdx"
      to: "/concepts/instructions-and-transactions"
      via: "Note callout"
---

<objective>
Write all Token Program Service pages: overview, Mint Creation grouped page, Holding Accounts grouped page, Mint method page, and ParseMint method page. Also update docs.json to remove individual stub entries and point to the grouped pages.

Purpose: Developers creating tokens or token accounts need to understand whether to use SPL Token or Token-2022 before choosing a method. The grouped pages make this choice obvious.

Output: 5 MDX pages (3 new/replaced using grouped page names + 2 existing stubs replaced) + docs.json update removing old stubs from the navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-program-services-and-error-reference/04-CONTEXT.md
@api-reference/account/get-account.mdx
@api-reference/transaction/overview.mdx
@snippets/token-program-field.mdx
@concepts/token-programs.mdx
</context>

<interfaces>
<!-- From ../protochain/lib/proto/protochain/solana/program/token/v1/service.proto -->
<!-- Go package: github.com/meshtrade/protochain/lib/go/protochain/solana/program/token/v1;token_v1 -->

CreateToken2022MintRequest:
  payer_pub_key (string), mint_pub_key (string), mint_authority_pub_key (string),
  freeze_authority_pub_key (string, optional), decimals (uint32),
  extensions (repeated Token2022Extension — only metadata supported currently; others COMING SOON)
CreateToken2022MintResponse:
  instructions (repeated SolanaInstruction — ordered: System::CreateAccount, pre-init extensions, initialize_mint, post-init extensions),
  lamports (uint64 — rent deposit), space (uint64 — initial allocation)

CreateSPLTokenMintRequest:
  payer_pub_key (string), mint_pub_key (string), mint_authority_pub_key (string),
  freeze_authority_pub_key (string, optional), decimals (uint32),
  metadata (MetaplexTokenMetadata, optional)
CreateSPLTokenMintResponse:
  instructions (repeated SolanaInstruction — System::CreateAccount + initialize_mint + optional Metaplex CreateMetadataAccountV3),
  lamports (uint64), space (uint64 — always 82 bytes for SPL Token)

ParseMintRequest: account_address (string)
ParseMintResponse:
  mint (MintInfo), token_program (TokenProgram enum), extensions (repeated Token2022Extension),
  metaplex_metadata (MetaplexTokenMetadata — only for SPL mints with Metaplex)

MintInfo fields: mint_authority_pub_key (string), freeze_authority_pub_key (string),
  decimals (uint32), supply (string), is_initialized (bool)

CreateToken2022HoldingAccountRequest:
  payer_pub_key (string), owner_pub_key (string), mint_pub_key (string),
  extensions (repeated Token2022HoldingAccountExtension)
CreateToken2022HoldingAccountResponse:
  instructions (repeated SolanaInstruction), lamports (uint64)

CreateSPLTokenHoldingAccountRequest:
  payer_pub_key (string), owner_pub_key (string), mint_pub_key (string)
CreateSPLTokenHoldingAccountResponse:
  instructions (repeated SolanaInstruction — single ATA creation instruction), lamports (uint64)

MintRequest:
  mint_pub_key (string), destination_owner_pub_key (string), amount (string — decimal string e.g. "1.5")
MintResponse: instruction (SolanaInstruction)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write Token Program overview + update docs.json for grouped navigation</name>
  <files>api-reference/token-program/overview.mdx, docs.json</files>
  <read_first>
    - api-reference/token-program/overview.mdx (current stub to replace)
    - api-reference/transaction/overview.mdx (overview page pattern)
    - concepts/token-programs.mdx (existing content — link to this, don't duplicate)
    - docs.json (Token Program Service group to update)
    - .planning/phases/04-program-services-and-error-reference/04-CONTEXT.md (D-03, D-04)
  </read_first>
  <action>
Part A — Write overview.mdx:

Frontmatter: title "Token Program Service", description "Build SPL Token and Token-2022 instructions for mint creation, holding account setup, minting tokens, and querying mint state."

Opening paragraph: Token Program Service provides instruction builders for the two Solana token programs. Each method returns a SolanaInstruction (or a list of instructions for multi-step setup). Before using any method, choose the right token program for your use case.

Note: Point to the Token Programs concept page for the SPL Token vs Token-2022 trade-offs — don't duplicate that content here. Link: "/concepts/token-programs".

Note callout: "Token Program methods that create instructions return SolanaInstruction objects. Add them to a transaction and use the Transaction Service to execute on-chain. See [Instructions & Transactions](/concepts/instructions-and-transactions)."

Methods table (columns: Method | Description | Grouped Page):
- CreateToken2022Mint / CreateSPLTokenMint | Create a new token mint. Token-2022 supports extensions; SPL Token supports Metaplex metadata. | /api-reference/token-program/mint-creation
- CreateToken2022HoldingAccount / CreateSPLTokenHoldingAccount | Create an Associated Token Account to hold tokens for a wallet. | /api-reference/token-program/holding-accounts
- Mint | Mint new tokens to a destination wallet (token-program agnostic). | /api-reference/token-program/mint
- ParseMint | Parse a mint account's state, detecting token program, authority, decimals, and extensions. | /api-reference/token-program/parse-mint

Part B — Update docs.json Token Program Service group:
Replace the current pages array (which lists all 7 individual stubs) with the grouped page structure:

```json
[
  "api-reference/token-program/overview",
  "api-reference/token-program/mint-creation",
  "api-reference/token-program/holding-accounts",
  "api-reference/token-program/mint",
  "api-reference/token-program/parse-mint"
]
```

Remove individual entries: "api-reference/token-program/create-token2022-mint", "api-reference/token-program/create-spl-token-mint", "api-reference/token-program/create-token2022-holding-account", "api-reference/token-program/create-spl-token-holding-account".

The grouped page files mint-creation.mdx and holding-accounts.mdx will be created in Tasks 2-3.
  </action>
  <verify>
    <automated>grep -c "mint-creation\|holding-accounts\|token-programs\|SolanaInstruction" /Users/kylesmith/Development/docs/api-reference/token-program/overview.mdx && grep "mint-creation" /Users/kylesmith/Development/docs/docs.json</automated>
  </verify>
  <acceptance_criteria>
    - overview.mdx contains link to "/concepts/token-programs"
    - overview.mdx contains link to "/concepts/instructions-and-transactions"
    - overview.mdx methods table contains "mint-creation" and "holding-accounts" links (grouped pages)
    - docs.json Token Program group contains "api-reference/token-program/mint-creation"
    - docs.json Token Program group does NOT contain "create-token2022-mint" or "create-spl-token-mint" as individual entries
    - docs.json is valid JSON (node -e "require('./docs.json')" passes)
    - overview.mdx does NOT contain "under construction" or "Coming soon"
  </acceptance_criteria>
  <done>Token Program overview written with SPL/Token-2022 framing and grouped method table. docs.json updated to grouped navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Write Mint Creation grouped page (CreateToken2022Mint + CreateSPLTokenMint)</name>
  <files>api-reference/token-program/mint-creation.mdx</files>
  <read_first>
    - api-reference/account/get-account.mdx (ResponseField + CodeGroup pattern)
    - .planning/phases/04-program-services-and-error-reference/04-CONTEXT.md (D-03: grouped SPL/Token-2022 pairs, D-06: Note callout, D-08, D-09, D-10, D-11)
    - concepts/token-programs.mdx (SPL vs Token-2022 concept — link to this, don't re-explain)
  </read_first>
  <action>
Create a new file at api-reference/token-program/mint-creation.mdx (this slug replaces the individual stubs which will be removed from docs.json).

Frontmatter: title "Create Mint", description "Build the complete instruction set to create and initialize a Token-2022 or SPL Token mint account in one call."

Opening paragraph: Both methods return the complete ordered instruction set needed to create a mint — you don't need to separately query rent, build a System Program create instruction, or initialize the mint. The key difference is which token program the mint belongs to: Token-2022 supports extensions (like metadata); SPL Token supports Metaplex metadata via a separate program.

Tip callout: "Not sure which token program to use? See [Token Programs](/concepts/token-programs) for the trade-offs."

Note callout (per D-06): "These methods return instruction lists. Add them in order to a transaction and use the Transaction Service to compile, sign, and submit. See [Instructions & Transactions](/concepts/instructions-and-transactions)."

---

## CreateToken2022Mint

Description: Creates and fully initializes a Token-2022 mint in one call. Returns the complete ordered instruction set: System::CreateAccount → extension pre-init instructions → initialize_mint → extension post-init instructions (e.g. token metadata, update fields).

### Request

ResponseField for each field (developer-friendly types per D-09):
- payer_pub_key: "Base58-encoded public key (string)" required — "Pays for account creation. Must be a signer."
- mint_pub_key: "Base58-encoded public key (string)" required — "The mint account to create. Must be a signer (new keypair)."
- mint_authority_pub_key: "Base58-encoded public key (string)" required — "The authority that can mint new tokens."
- freeze_authority_pub_key: "Base58-encoded public key (string)" — "Optional. Authority that can freeze token accounts. Omit to disable freeze."
- decimals: "uint32" required — "Decimal precision. Common values: 9 (SOL-like), 6 (USDC-like), 0 (NFTs)."
- extensions: "Token2022Extension[] (repeated)" — "Optional. Token-2022 extensions to enable at initialization. Extensions cannot be added after mint creation. Currently supported: metadata extension. Transfer hooks, transfer fees, and other extensions are coming soon."
  Use Expandable for Token2022Extension with the metadata subfield:
  - metadata: "Token2022ExtensionMetadata" — name (string), symbol (string), uri (string), additional_metadata repeated (key-value pairs)

### Response

ResponseField:
- instructions: "SolanaInstruction[] (repeated)" required — "Ordered instructions. Submit in this exact order."
- lamports: "uint64" — "Lamports deposited for rent exemption. Covers base mint size plus variable extension data."
- space: "uint64" — "Initial bytes allocated by System::CreateAccount. Token-2022 may realloc beyond this for variable-length extension data."

### Code Examples

CodeGroup Go/Rust/TypeScript showing CreateToken2022Mint call. Include comment: `// Add all instructions to your transaction in order`. Show a simple case (no extensions) and note extensions are appended to the slice.

---

## CreateSPLTokenMint

Description: Creates and fully initializes a legacy SPL Token mint. Returns System::CreateAccount + initialize_mint + optional Metaplex CreateMetadataAccountV3. Space is always 82 bytes for SPL Token mints.

### Request

- payer_pub_key: "Base58-encoded public key (string)" required — same as above
- mint_pub_key: "Base58-encoded public key (string)" required — same
- mint_authority_pub_key: "Base58-encoded public key (string)" required — same
- freeze_authority_pub_key: "Base58-encoded public key (string)" — optional, same
- decimals: "uint32" required — same
- metadata: "MetaplexTokenMetadata (object)" — "Optional. Metaplex on-chain metadata. When provided, appends CreateMetadataAccountV3 instruction."
  Use Expandable for MetaplexTokenMetadata:
  - name (string), symbol (string), uri (string, metadata JSON URL)

### Response

- instructions: "SolanaInstruction[] (repeated)" — "Ordered: System::CreateAccount, initialize_mint, and optionally CreateMetadataAccountV3."
- lamports: "uint64" — rent exemption lamports
- space: "uint64" — "Always 82 for SPL Token mints."

### Code Examples

CodeGroup Go/Rust/TypeScript showing CreateSPLTokenMint. Include comment: `// Add all instructions to your transaction in order`.
  </action>
  <verify>
    <automated>grep -c "CreateToken2022Mint\|CreateSPLTokenMint\|instructions-and-transactions\|payer_pub_key\|decimals" /Users/kylesmith/Development/docs/api-reference/token-program/mint-creation.mdx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at api-reference/token-program/mint-creation.mdx
    - File contains "CreateToken2022Mint" section header
    - File contains "CreateSPLTokenMint" section header
    - File contains Note callout linking to "/concepts/instructions-and-transactions"
    - File contains Tip callout linking to "/concepts/token-programs"
    - File contains "payer_pub_key" ResponseField
    - File contains "decimals" ResponseField
    - File contains CodeGroup with Go, Rust, TypeScript for both methods
    - File does NOT contain "under construction" or "Coming soon"
  </acceptance_criteria>
  <done>mint-creation.mdx covers both CreateToken2022Mint and CreateSPLTokenMint with complete field documentation, extension support, and code examples. SPL vs Token-2022 choice is framed at the top.</done>
</task>

<task type="auto">
  <name>Task 3: Write Holding Accounts, Mint, and ParseMint pages</name>
  <files>api-reference/token-program/holding-accounts.mdx, api-reference/token-program/mint.mdx, api-reference/token-program/parse-mint.mdx</files>
  <read_first>
    - api-reference/token-program/mint.mdx (current stub to replace)
    - api-reference/token-program/parse-mint.mdx (current stub to replace)
    - api-reference/token-program/mint-creation.mdx (use as template — just written in Task 2)
    - .planning/phases/04-program-services-and-error-reference/04-CONTEXT.md (D-03, D-04, D-06)
  </read_first>
  <action>
Write three files:

---

### FILE 1: api-reference/token-program/holding-accounts.mdx (new file)

Frontmatter: title "Create Holding Account", description "Build the complete instruction set to create a Token-2022 or SPL Token Associated Token Account for a wallet."

Opening: Both methods create an Associated Token Account (ATA) — the standard account type that holds tokens for a wallet. Choose the method matching your mint's token program.

Note callout: "These methods return instruction lists. Add them to a transaction and use the Transaction Service. See [Instructions & Transactions](/concepts/instructions-and-transactions)."

## CreateToken2022HoldingAccount

Description: Creates a Token-2022 Associated Token Account with optional extensions. Returns ATA creation instruction plus reallocate and extension-init instructions for each requested extension.

### Request
- payer_pub_key: "Base58-encoded public key (string)" required — "Pays for ATA creation. Must be a signer."
- owner_pub_key: "Base58-encoded public key (string)" required — "The wallet that will own this token account."
- mint_pub_key: "Base58-encoded public key (string)" required — "The Token-2022 mint this account will hold tokens for."
- extensions: "Token2022HoldingAccountExtension[] (repeated)" — "Optional. Extensions to enable on the ATA. Currently supported: MemoTransfer (requires memo on all incoming transfers)."

### Response
- instructions: "SolanaInstruction[] (repeated)" — "Ordered instructions. Includes ATA creation plus reallocate+init for each extension."
- lamports: "uint64" — "Rent-exempt minimum for the final account size including all extensions."

### Code Examples
CodeGroup Go/Rust/TypeScript. End with `// Add all instructions to your transaction in order`.

---

## CreateSPLTokenHoldingAccount

Description: Creates a legacy SPL Token Associated Token Account. Returns a single ATA creation instruction.

### Request
- payer_pub_key: "Base58-encoded public key (string)" required — "Pays for ATA creation. Must be a signer."
- owner_pub_key: "Base58-encoded public key (string)" required — "The wallet that will own this token account."
- mint_pub_key: "Base58-encoded public key (string)" required — "The SPL Token mint this account will hold tokens for."

### Response
- instructions: "SolanaInstruction[] (repeated)" — "A single ATA creation instruction."
- lamports: "uint64" — "Rent-exempt minimum for the ATA."

### Code Examples
CodeGroup Go/Rust/TypeScript. End with `// Add instruction to your transaction`.

---

### FILE 2: api-reference/token-program/mint.mdx (replace stub)

Frontmatter: title "Mint", description "Build a MintToChecked instruction to mint tokens to a destination wallet. Token-program agnostic."

Opening: The Mint method reads the mint account on-chain to determine the token program, mint authority, and decimal precision automatically — you only supply the mint address, destination wallet, and amount as a human-readable decimal string.

Note callout: "This method returns a SolanaInstruction. Add it to a transaction and use the Transaction Service. See [Instructions & Transactions](/concepts/instructions-and-transactions)."

Warning callout: "Pass the destination **owner's system account address** in `destination_owner_pub_key` — not the Associated Token Account (ATA) address. The ATA is derived automatically."

### Request
- mint_pub_key: "Base58-encoded public key (string)" required — "The token mint to mint from."
- destination_owner_pub_key: "Base58-encoded public key (string)" required — "The system account (wallet) that owns the destination token account. The ATA is derived from this address and the mint automatically. Do NOT pass the ATA address."
- amount: "string (decimal)" required — "Human-readable token amount. Express in whole-token units using the mint's decimal precision. Examples: \"1.5\" for 1.5 tokens, \"1000\" for 1000 tokens, \"0.000001\" for 1 base unit (6-decimal mint)."

### Response
- instruction: "SolanaInstruction (object)" — "The MintToChecked instruction."

### Code Examples
CodeGroup Go/Rust/TypeScript with Mint call. Show amount as a decimal string e.g. "10.0". End with `// Add instruction to your transaction`.

---

### FILE 3: api-reference/token-program/parse-mint.mdx (replace stub)

Frontmatter: title "ParseMint", description "Parse a mint account's on-chain state — including authority, decimals, supply, token program, and Token-2022 extensions."

Opening: ParseMint reads a mint account and returns its structured state. It detects whether the mint belongs to the SPL Token or Token-2022 program and returns the appropriate extension or metadata data.

Note: ParseMint is a **query** method — it does not return a SolanaInstruction. No transaction is needed.

### Request
- account_address: "Base58-encoded public key (string)" required — "The mint account address to parse."

### Response

ResponseField for each:
- mint: "MintInfo (object)" required — "Core mint account fields."
  Expandable with MintInfo fields:
  - mint_authority_pub_key: "Base58-encoded public key (string)" — "Authority that can mint new tokens."
  - freeze_authority_pub_key: "Base58-encoded public key (string)" — "Authority that can freeze token accounts. Empty if freeze is disabled."
  - decimals: "uint32" — "Decimal precision of the token."
  - supply: "string" — "Current total supply in base units (as a string to avoid integer overflow)."
  - is_initialized: "bool" — "True if the mint account has been initialized."
- token_program: "TokenProgram (enum)" — "TOKEN_PROGRAM_SPL_TOKEN or TOKEN_PROGRAM_TOKEN_2022. Indicates which token program owns this mint."
- extensions: "Token2022Extension[] (repeated)" — "Token-2022 extensions on this mint. Empty for SPL Token mints."
- metaplex_metadata: "MetaplexTokenMetadata (object)" — "Metaplex on-chain metadata. Only present for SPL Token mints created with CreateSPLTokenMint and metadata provided."

### Code Examples
CodeGroup Go/Rust/TypeScript with ParseMint call.
  </action>
  <verify>
    <automated>grep -c "CreateSPLTokenHoldingAccount\|CreateToken2022HoldingAccount" /Users/kylesmith/Development/docs/api-reference/token-program/holding-accounts.mdx && grep -c "destination_owner_pub_key\|ATA" /Users/kylesmith/Development/docs/api-reference/token-program/mint.mdx && grep -c "MintInfo\|token_program\|extensions" /Users/kylesmith/Development/docs/api-reference/token-program/parse-mint.mdx</automated>
  </verify>
  <acceptance_criteria>
    - holding-accounts.mdx exists and contains both "CreateToken2022HoldingAccount" and "CreateSPLTokenHoldingAccount" sections
    - holding-accounts.mdx contains Note callout linking to "/concepts/instructions-and-transactions"
    - mint.mdx contains Warning callout about destination_owner_pub_key (not ATA address)
    - mint.mdx contains "decimal" description in the amount field (human-readable decimal string)
    - mint.mdx contains Note callout linking to "/concepts/instructions-and-transactions"
    - parse-mint.mdx contains "MintInfo" in an Expandable
    - parse-mint.mdx contains "token_program" ResponseField
    - parse-mint.mdx does NOT contain a Note callout about instructions (it's a query, not an instruction builder)
    - All three files contain CodeGroup with Go, Rust, TypeScript tabs
    - None of the three files contain "under construction" or "Coming soon"
  </acceptance_criteria>
  <done>holding-accounts.mdx covers both holding account creation methods. mint.mdx documents the token-program-agnostic Mint with the ATA owner gotcha. parse-mint.mdx documents MintInfo and extension response fields.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -r "Coming soon\|under construction" api-reference/token-program/` — must return no matches
2. `node -e "require('./docs.json')"` from repo root — valid JSON
3. `grep "mint-creation" docs.json` — must match (grouped page in navigation)
4. `grep "create-token2022-mint" docs.json` — must return no matches (old individual stub removed)
5. `ls api-reference/token-program/holding-accounts.mdx api-reference/token-program/mint-creation.mdx` — both files must exist
6. `grep "destination_owner_pub_key" api-reference/token-program/mint.mdx` — must match (critical field)
</verification>

<success_criteria>
- Token Program overview explains SPL vs Token-2022 choice before listing methods, links to concept page
- Mint Creation page covers both CreateToken2022Mint and CreateSPLTokenMint with extension support documented
- Holding Accounts page covers both holding account creation methods
- Mint page documents token-program-agnostic minting with the owner-vs-ATA warning prominent
- ParseMint page documents MintInfo and all response fields including token_program detection
- docs.json updated to grouped navigation (5 entries instead of 7)
</success_criteria>

<output>
After completion, create `.planning/phases/04-program-services-and-error-reference/04-03-SUMMARY.md`
</output>
