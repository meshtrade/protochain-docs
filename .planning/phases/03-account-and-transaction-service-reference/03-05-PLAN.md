---
phase: 03-account-and-transaction-service-reference
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - api-reference/errors.mdx
autonomous: true
requirements:
  - TXN-07
  - TXN-08
  - TXN-09

must_haves:
  truths:
    - "The error page has a scannable table covering all 24 TransactionErrorCode values with Code, Description, Retryable, and Remediation columns"
    - "The error codes are grouped into three categories: Permanent Failures, Temporary Failures, and Indeterminate"
    - "TransactionSubmissionCertainty levels are documented with handling guidance for each scenario"
    - "The page is the definitive destination for developers who hit a submission error — not an essay but a reference table"
  artifacts:
    - path: "api-reference/errors.mdx"
      provides: "Error reference page for TransactionErrorCode and TransactionSubmissionCertainty"
      contains: "| Error Code | Description | Retryable | Remediation |"
    - path: "api-reference/errors.mdx"
      contains: "TRANSACTION_ERROR_CODE_BLOCKHASH_NOT_FOUND"
    - path: "api-reference/errors.mdx"
      contains: "TRANSACTION_SUBMISSION_CERTAINTY_UNKNOWN_RESOLVABLE"
  key_links:
    - from: "api-reference/errors.mdx"
      to: "api-reference/transaction/submit-transaction"
      via: "opening prose or section header"
    - from: "api-reference/errors.mdx"
      to: "api-reference/transaction/monitor-transaction"
      via: "TransactionSubmissionCertainty handling guidance"
    - from: "api-reference/errors.mdx"
      to: "api-reference/transaction/compile-transaction"
      via: "BLOCKHASH_NOT_FOUND remediation"
---

<objective>
Write the error reference page documenting all 24 TransactionErrorCode values and 5 TransactionSubmissionCertainty levels. This is the single destination developers visit when a submission fails.

Purpose: Developers can quickly look up any error code, determine whether to retry, and know exactly what remediation action to take.

Output: 1 MDX file replacing the stub.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-account-and-transaction-service-reference/03-CONTEXT.md
@.planning/phases/03-account-and-transaction-service-reference/03-RESEARCH.md

<interfaces>
<!-- Error page structure (per D-07): table with Code | Description | Retryable | Remediation -->
<!-- Error page also documents TransactionSubmissionCertainty (per D-08) -->

<!-- TransactionErrorCode — 24 values in 3 groups: -->

<!-- PERMANENT FAILURES (not retryable, transaction NOT sent): -->
<!--   TRANSACTION_ERROR_CODE_UNSPECIFIED = 0 -->
<!--   TRANSACTION_ERROR_CODE_INVALID_TRANSACTION = 1 — malformed structure, rebuild -->
<!--   TRANSACTION_ERROR_CODE_INVALID_SIGNATURE = 2 — missing/invalid sigs, re-sign -->
<!--   TRANSACTION_ERROR_CODE_SIGNATURE_VERIFICATION_FAILED = 3 — crypto failed, re-sign -->
<!--   TRANSACTION_ERROR_CODE_ACCOUNT_NOT_FOUND = 4 — account doesn't exist, check address -->
<!--   TRANSACTION_ERROR_CODE_INVALID_ACCOUNT = 5 — account in invalid state, check setup -->
<!--   TRANSACTION_ERROR_CODE_PROGRAM_ERROR = 6 — program execution failed, check instructions -->
<!--   TRANSACTION_ERROR_CODE_INSTRUCTION_ERROR = 7 — specific instruction failed, check instruction -->
<!--   TRANSACTION_ERROR_CODE_PRECOMPILE_VERIFICATION_FAILED = 8 — precompile sig failed, re-sign -->
<!--   TRANSACTION_ERROR_CODE_INVALID_BLOCKHASH_FORMAT = 9 — blockhash not valid Base58, recompile -->
<!--   TRANSACTION_ERROR_CODE_TRANSACTION_TOO_LARGE = 10 — exceeds 1232 bytes, reduce instructions -->
<!--   TRANSACTION_ERROR_CODE_BLOCKHASH_NOT_FOUND = 11 — blockhash expired, recompile -->

<!-- TEMPORARY FAILURES (retryable, transaction NOT sent): -->
<!--   TRANSACTION_ERROR_CODE_INSUFFICIENT_FUNDS = 20 — add SOL to fee payer, retry same tx -->
<!--   TRANSACTION_ERROR_CODE_ACCOUNT_IN_USE = 21 — wait for unlock, retry same tx -->
<!--   TRANSACTION_ERROR_CODE_WOULD_EXCEED_BLOCK_LIMIT = 22 — retry on next block -->
<!--   TRANSACTION_ERROR_CODE_TRANSIENT_SIMULATION_FAILURE = 23 — retry -->

<!-- INDETERMINATE (transaction may or may not have been sent): -->
<!--   TRANSACTION_ERROR_CODE_NETWORK_ERROR = 40 -->
<!--   TRANSACTION_ERROR_CODE_TIMEOUT = 41 — DANGEROUS, tx might have been sent -->
<!--   TRANSACTION_ERROR_CODE_NODE_UNHEALTHY = 42 -->
<!--   TRANSACTION_ERROR_CODE_RATE_LIMITED = 43 -->
<!--   TRANSACTION_ERROR_CODE_RPC_ERROR = 44 -->
<!--   TRANSACTION_ERROR_CODE_CONNECTION_FAILED = 45 — likely NOT sent -->
<!--   TRANSACTION_ERROR_CODE_REQUEST_FAILED = 46 -->
<!--   TRANSACTION_ERROR_CODE_UNKNOWN = 47 -->

<!-- TransactionSubmissionCertainty — 5 values: -->
<!--   TRANSACTION_SUBMISSION_CERTAINTY_UNSPECIFIED = 0 — treat as UNKNOWN -->
<!--   TRANSACTION_SUBMISSION_CERTAINTY_NOT_SUBMITTED = 1 — 100% certain NOT sent, safe to rebuild -->
<!--   TRANSACTION_SUBMISSION_CERTAINTY_SUBMITTED = 2 — 100% certain WAS sent, use MonitorTransaction -->
<!--   TRANSACTION_SUBMISSION_CERTAINTY_UNKNOWN_RESOLVABLE = 3 — wait for blockhash expiry, then query on-chain -->
<!--   TRANSACTION_SUBMISSION_CERTAINTY_UNKNOWN = 4 — treat conservatively, wait + check -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Error reference page</name>
  <files>api-reference/errors.mdx</files>
  <read_first>
    - api-reference/errors.mdx (current stub — verify it exists, note its content)
    - .planning/phases/03-account-and-transaction-service-reference/03-RESEARCH.md (full error type inventory sections: "TransactionError message fields", "TransactionErrorCode enum — all 24 values", "TransactionSubmissionCertainty enum")
    - .planning/phases/03-account-and-transaction-service-reference/03-CONTEXT.md (D-06: dedicated error page, D-07: table format with Code|Description|Retryable|Remediation, D-08: TransactionSubmissionCertainty on error page)
  </read_first>
  <action>
Write the complete error reference page. Scannable reference, not an essay.

Frontmatter:
```yaml
title: "Error Reference"
description: "TransactionErrorCode values, retryability classification, and remediation guidance."
```

Opening paragraph (2 sentences):
"This page documents every `TransactionErrorCode` returned in `SubmitTransaction` errors and how to handle each one. Error codes are grouped by their retryability and whether the transaction was sent to the network."

---

## TransactionErrorCode

"The `code` field of a `TransactionError` object. Determines whether retrying the same transaction is safe, and what remediation action to take."

Note about the three groups:
```
<Note>
  Error codes are grouped into three categories:
  - **Permanent failures** — transaction was NOT sent; rebuild required before retrying
  - **Temporary failures** — transaction was NOT sent; same transaction can be retried
  - **Indeterminate** — unknown whether the transaction was sent; check `structured_error.certainty`
</Note>
```

### Permanent Failures

"The transaction was NOT sent to the network. The error condition is not recoverable with the same transaction — rebuild and re-sign before retrying."

Table:
| Error Code | Description | Retryable | Remediation |
|------------|-------------|-----------|-------------|
| `TRANSACTION_ERROR_CODE_INVALID_TRANSACTION` | Malformed transaction structure. | No | Rebuild the transaction from scratch with valid instructions. |
| `TRANSACTION_ERROR_CODE_INVALID_SIGNATURE` | Missing or invalid signatures. | No | Verify all required signers have called SignTransaction. |
| `TRANSACTION_ERROR_CODE_SIGNATURE_VERIFICATION_FAILED` | Cryptographic signature verification failed. | No | Re-sign the transaction with the correct private keys. |
| `TRANSACTION_ERROR_CODE_ACCOUNT_NOT_FOUND` | A required account does not exist on-chain. | No | Verify all account addresses in the transaction's instructions. |
| `TRANSACTION_ERROR_CODE_INVALID_ACCOUNT` | An account exists but is in an invalid state for this operation. | No | Inspect the account's owner, executable flag, and data for the specific program's requirements. |
| `TRANSACTION_ERROR_CODE_PROGRAM_ERROR` | Program execution failed. | No | Review the instruction inputs and program requirements. Check `meta_error_message` from GetTransaction for details. |
| `TRANSACTION_ERROR_CODE_INSTRUCTION_ERROR` | A specific instruction failed during execution. | No | Identify the failing instruction and correct the inputs. Check simulation logs for details. |
| `TRANSACTION_ERROR_CODE_PRECOMPILE_VERIFICATION_FAILED` | Precompile (e.g., ed25519 or secp256k1) signature verification failed. | No | Re-sign the transaction with the correct keys. |
| `TRANSACTION_ERROR_CODE_INVALID_BLOCKHASH_FORMAT` | The transaction's blockhash is not valid Base58. | No | Call CompileTransaction again — the service will fetch a valid blockhash automatically. |
| `TRANSACTION_ERROR_CODE_TRANSACTION_TOO_LARGE` | Transaction exceeds Solana's 1232-byte size limit. | No | Reduce the number of instructions or accounts per transaction. Split into multiple transactions if necessary. |
| `TRANSACTION_ERROR_CODE_BLOCKHASH_NOT_FOUND` | The transaction's blockhash has expired (~150 slots after creation). | No | Call [CompileTransaction](/api-reference/transaction/compile-transaction) to recompile with a fresh blockhash, then re-sign. |

### Temporary Failures

"The transaction was NOT sent to the network. The same signed transaction can be retried without rebuilding."

Table:
| Error Code | Description | Retryable | Remediation |
|------------|-------------|-----------|-------------|
| `TRANSACTION_ERROR_CODE_INSUFFICIENT_FUNDS` | The fee payer account has insufficient SOL to pay transaction fees. | Yes | Fund the fee payer account, then retry the same transaction. |
| `TRANSACTION_ERROR_CODE_ACCOUNT_IN_USE` | An account required by the transaction is locked by another in-flight transaction. | Yes | Wait a few seconds for the conflicting transaction to process, then retry. |
| `TRANSACTION_ERROR_CODE_WOULD_EXCEED_BLOCK_LIMIT` | The current block is at compute capacity. | Yes | Retry on the next block (~400ms). |
| `TRANSACTION_ERROR_CODE_TRANSIENT_SIMULATION_FAILURE` | Temporary simulation error — not related to the transaction itself. | Yes | Retry immediately. |

### Indeterminate

"The service cannot determine with certainty whether the transaction was sent to the network. Use `structured_error.certainty` (a `TransactionSubmissionCertainty` value) to determine the recovery strategy."

Warning callout:
```
<Warning>
  `TRANSACTION_ERROR_CODE_TIMEOUT` is the most dangerous indeterminate error. The request timed out, but the transaction **may have been broadcast** before the timeout. Do not assume it was not sent. Always check `certainty` and wait for blockhash expiry before deciding to resubmit.
</Warning>
```

Table:
| Error Code | Description | Likely Sent? | Remediation |
|------------|-------------|--------------|-------------|
| `TRANSACTION_ERROR_CODE_NETWORK_ERROR` | Network error during submission — could have failed at any point. | Unknown | Check `certainty`. Wait for blockhash expiry, then query on-chain. |
| `TRANSACTION_ERROR_CODE_TIMEOUT` | Request timed out — transaction may have been broadcast. | Possibly | **Do not resubmit immediately.** Check `certainty` and `blockhash_expiry_slot`. Query on-chain after expiry. |
| `TRANSACTION_ERROR_CODE_NODE_UNHEALTHY` | The RPC node was unhealthy — may have received the transaction. | Possibly | Check `certainty`. Use a different RPC node and verify on-chain. |
| `TRANSACTION_ERROR_CODE_RATE_LIMITED` | Rate limited — whether the transaction was sent depends on where limiting occurred. | Unknown | Check `certainty`. Back off and verify on-chain before retrying. |
| `TRANSACTION_ERROR_CODE_RPC_ERROR` | Generic RPC failure. | Unknown | Check `certainty`. Verify on-chain before retrying. |
| `TRANSACTION_ERROR_CODE_CONNECTION_FAILED` | Connection could not be established — transaction was likely NOT sent. | Likely no | Check `certainty`. Safe to retry if `certainty` is NOT_SUBMITTED. |
| `TRANSACTION_ERROR_CODE_REQUEST_FAILED` | HTTP/transport layer request failed. | Unknown | Check `certainty`. Verify on-chain before retrying. |
| `TRANSACTION_ERROR_CODE_UNKNOWN` | Unclassified error. | Unknown | Check `certainty`. Treat conservatively. |

---

## TransactionSubmissionCertainty

"The `certainty` field on a `TransactionError` object. Use this to determine whether it is safe to resubmit a transaction after an indeterminate error."

Table:
| Certainty | Value | Meaning | Handling |
|-----------|-------|---------|----------|
| `TRANSACTION_SUBMISSION_CERTAINTY_NOT_SUBMITTED` | 1 | 100% certain the transaction was NOT sent to the network. | Safe to rebuild and resubmit immediately. |
| `TRANSACTION_SUBMISSION_CERTAINTY_SUBMITTED` | 2 | 100% certain the transaction WAS sent to the network. | Do not resubmit. Use [MonitorTransaction](/api-reference/transaction/monitor-transaction) to track the outcome. |
| `TRANSACTION_SUBMISSION_CERTAINTY_UNKNOWN_RESOLVABLE` | 3 | Uncertain, but resolvable by waiting. | Wait until after `blockhash_expiry_slot`. Then query on-chain — if the transaction is not found, it is safe to recompile and resubmit. |
| `TRANSACTION_SUBMISSION_CERTAINTY_UNKNOWN` | 4 | Uncertain and not easily resolvable. | Treat conservatively: wait for blockhash expiry, query on-chain, and only resubmit if the transaction is definitively absent. |
| `TRANSACTION_SUBMISSION_CERTAINTY_UNSPECIFIED` | 0 | Not set (default). | Treat as UNKNOWN. |

Note callout after the table:
```
<Note>
  `blockhash_expiry_slot` on the `TransactionError` object indicates when the transaction's blockhash expires (approximately 150 slots after compilation). Once the blockhash has expired, any transaction that was broadcast but not confirmed is guaranteed to be dead — it is then safe to recompile and resubmit.
</Note>
```
  </action>
  <verify>
    grep -c "TRANSACTION_ERROR_CODE_" /Users/kylesmith/Development/docs/api-reference/errors.mdx
    # Should be >= 24 (one per error code)
    grep -c "TRANSACTION_SUBMISSION_CERTAINTY_" /Users/kylesmith/Development/docs/api-reference/errors.mdx
    # Should be >= 5
  </verify>
  <acceptance_criteria>
    - File contains "| Error Code | Description | Retryable | Remediation |" (table header present per D-07)
    - File contains "TRANSACTION_ERROR_CODE_BLOCKHASH_NOT_FOUND" (blockhash expiry error documented)
    - File contains "TRANSACTION_ERROR_CODE_TIMEOUT" with a Warning about dangerous behavior
    - File contains "TRANSACTION_ERROR_CODE_INSUFFICIENT_FUNDS" in Temporary Failures section
    - File contains "TRANSACTION_SUBMISSION_CERTAINTY_UNKNOWN_RESOLVABLE" (per D-08)
    - File contains "TRANSACTION_SUBMISSION_CERTAINTY_NOT_SUBMITTED" (per D-08)
    - File contains "/api-reference/transaction/monitor-transaction" link
    - File contains "/api-reference/transaction/compile-transaction" link (in BLOCKHASH_NOT_FOUND remediation)
    - File contains "Permanent" and "Temporary" and "Indeterminate" (three groups per research)
    - File does NOT contain "Coming soon"
    - title frontmatter is "Error Reference"
  </acceptance_criteria>
  <done>Error reference page is complete with all 24 TransactionErrorCode values in a scannable table (grouped by category), TransactionSubmissionCertainty levels with handling guidance, and links to MonitorTransaction and CompileTransaction for recovery actions.</done>
</task>

</tasks>

<verification>
```bash
# Stub replaced
grep -L "Coming soon\|under construction" /Users/kylesmith/Development/docs/api-reference/errors.mdx

# All error code groups present
grep "Permanent Failures\|Temporary Failures\|Indeterminate" /Users/kylesmith/Development/docs/api-reference/errors.mdx

# Both type tables present
grep "TransactionErrorCode\|TransactionSubmissionCertainty" /Users/kylesmith/Development/docs/api-reference/errors.mdx

# All 24 error codes present
grep -c "TRANSACTION_ERROR_CODE_" /Users/kylesmith/Development/docs/api-reference/errors.mdx
```
</verification>

<success_criteria>
- Error reference page covers all 24 TransactionErrorCode values in a table with Code | Description | Retryable | Remediation columns (per D-07)
- Error codes are grouped: Permanent Failures, Temporary Failures, Indeterminate
- TIMEOUT error has a Warning callout about dangerous resubmission behavior
- TransactionSubmissionCertainty levels are documented with handling guidance (per D-08)
- Page links to MonitorTransaction for SUBMITTED certainty handling
- Page links to CompileTransaction for BLOCKHASH_NOT_FOUND remediation
- No prose walls — tables used throughout for scannability
</success_criteria>

<output>
After completion, create `.planning/phases/03-account-and-transaction-service-reference/03-05-SUMMARY.md`
</output>
